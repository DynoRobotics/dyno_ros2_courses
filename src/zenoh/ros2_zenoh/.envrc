# Automatically activate Nix development environment
# Install direnv: https://direnv.net/
# Run once: direnv allow

# Use Nix flake
use flake

# Load .env file if it exists (for secrets, local config)
dotenv_if_exists .env

# Optional: Watch additional files
watch_file flake.nix
watch_file flake.lock


