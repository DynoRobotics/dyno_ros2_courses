This cache is used to shorten the late build steps for installing ROS2 package dependencies from this project.

This way we make sure that any dependences listed will be evaluated and auto installed after any change in the repo, but the cached packages will not have to be reinstalled.